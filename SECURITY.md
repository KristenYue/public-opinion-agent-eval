# 安全与隐私规则

1. 禁止将Cookie、API Key、`.env`或用户凭证复制到本项目。
2. `data/raw_private/`仅供本地处理，不上传Git。
3. 公开数据只保留分析所需字段，删除用户名、用户ID和评论ID。
4. 处理后的`sample_id`由事件名与文本哈希生成，不使用平台用户标识。
5. 已发现旧RAG示例中存在明文API Key，该文件不得迁移，原Key应在服务商控制台撤销。
